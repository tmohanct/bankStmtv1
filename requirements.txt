# Core data and Excel output
pandas>=2.2.0,<3.0.0
openpyxl>=3.1.0,<4.0.0

# PDF extraction
pdfplumber>=0.11.0,<1.0.0
pymupdf>=1.27.1,<2.0.0
Pillow>=12.0.0,<13.0.0

# OCR support
# Note: the Python package is `pytesseract`, but the Tesseract OCR app
# must also be installed separately on the machine for OCR-based parsers.
pytesseract>=0.3.13,<1.0.0
