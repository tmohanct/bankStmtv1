# bankStmtv1

Use [SETUP_WINDOWS.md](SETUP_WINDOWS.md) to install and run this project on another Windows machine.

For a fresh Windows machine, run `.\install_fresh_machine.bat`.

To create a clean shareable zip package for another machine, run `.\build_windows_package.ps1`.
